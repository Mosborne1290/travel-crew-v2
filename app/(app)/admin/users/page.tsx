import { redirect } from "next/navigation";import { requireUser,getCurrentRole } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";import { OwnerUserManagement } from "@/components/owner-user-management";
export default async function OwnerUsersPage(){const user=await requireUser();const role=await getCurrentRole(user.id);if(role!=="owner")redirect("/dashboard");const supabase=await createClient();const [{data:profiles},{data:roles}]=await Promise.all([supabase.from("profiles").select("id,display_name,first_name,last_name,email,account_disabled,last_seen_at").order("display_name"),supabase.from("user_roles").select("user_id,role")]);const users=(profiles??[]).map(p=>({id:p.id,display_name:p.display_name||[p.first_name,p.last_name].filter(Boolean).join(" ")||p.email||"Traveller",email:p.email||null,role:(roles??[]).find(r=>r.user_id===p.id)?.role||"member",account_disabled:Boolean(p.account_disabled),last_seen_at:p.last_seen_at||null}));return <><header className="page-header"><div><h1>Owner Administration</h1><div className="muted">Manage who can access Travel Crew.</div></div></header><OwnerUserManagement currentUserId={user.id} initialUsers={users}/></>}
