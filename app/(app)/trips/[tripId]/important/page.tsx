import { notFound } from "next/navigation";
import { requireUser } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";import type { Trip } from "@/lib/types";import { TripWorkspaceHeader } from "@/components/trip-workspace-header";import { TripImportantInfo } from "@/components/trip-important-info";
export default async function ImportantPage({params}:{params:Promise<{tripId:string}>}){const {tripId}=await params;const user=await requireUser();const supabase=await createClient();const [{data:trip},{data:info}]=await Promise.all([supabase.from("trips").select("*").eq("id",tripId).maybeSingle(),supabase.from("trip_important_info").select("*").eq("trip_id",tripId).maybeSingle()]);if(!trip)notFound();return <><TripWorkspaceHeader trip={trip as Trip} active="important"/><TripImportantInfo tripId={tripId} userId={user.id} initial={info}/></>}
