import { notFound } from "next/navigation";import { createClient } from "@/lib/supabase/server";import type { Trip } from "@/lib/types";import { TripWorkspaceHeader } from "@/components/trip-workspace-header";import { OfflineQuickAdd } from "@/components/offline-quick-add";
export default async function OfflineToolsPage({params}:{params:Promise<{tripId:string}>}){const {tripId}=await params;const supabase=await createClient();const {data:trip}=await supabase.from("trips").select("*").eq("id",tripId).maybeSingle();if(!trip)notFound();return <><TripWorkspaceHeader trip={trip as Trip} active="offline"/><OfflineQuickAdd tripId={tripId} homeCurrency={trip.home_currency||"AUD"}/></>}
