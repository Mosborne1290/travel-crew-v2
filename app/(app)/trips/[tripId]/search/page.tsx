import { notFound } from "next/navigation";import { createClient } from "@/lib/supabase/server";import type { Trip } from "@/lib/types";import { TripWorkspaceHeader } from "@/components/trip-workspace-header";import { TripSearch } from "@/components/trip-search";
export default async function SearchPage({params}:{params:Promise<{tripId:string}>}){const {tripId}=await params;const supabase=await createClient();const {data:trip}=await supabase.from("trips").select("*").eq("id",tripId).maybeSingle();if(!trip)notFound();return <><TripWorkspaceHeader trip={trip as Trip} active="search"/><TripSearch tripId={tripId}/></>}
